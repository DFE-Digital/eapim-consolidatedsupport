<policies>
    <inbound>
        <base />
        <set-backend-service base-url="{{createaccountlogicapp-baseUrl}}" />
        <set-method>POST</set-method>
        <rewrite-uri template="{{createaccountlogicapp-rewriteUrl}}" />
        <set-header name="Ocp-Apim-Subscription-Key" exists-action="delete" />
        <set-header name="Authorization" exists-action="delete" />
        <send-one-way-request mode="new">
            <set-url>{{tlevel-employer-created-handler-url}}</set-url>
            <set-method>POST</set-method>
            <set-header name="Content-Type" exists-action="override">
                <value>application/Json</value>
            </set-header>
            <set-body>@{ return context.Request.Body.As&lt;string&gt;();}</set-body>
        </send-one-way-request>        
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>
