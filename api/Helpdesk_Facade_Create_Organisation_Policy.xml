<policies>
    <inbound>
        <base />
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <set-header name="Entity-Id" exists-action="override">
            <value>@{
                    string toReturn = null;

                    JObject parsedBodyContent = context.Response.Body.As<JObject>();

                    toReturn = (string)parsedBodyContent["organization"]["id"];

                    return toReturn;
                }</value>
        </set-header>
        <base />
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>
